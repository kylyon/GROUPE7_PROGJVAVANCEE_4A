using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class PlayerSelection : MonoBehaviour
{
    public Image p1Controller;
    public Image p2Controller;
    

    // Update is called once per frame
    void Update()
    {
        /*if (Input.GetKeyDown(KeyCode.Q) && p1Controller.rectTransform.anchoredPosition.x >= 0)
        {
            if (GameManager.Instance().SetPlayerHitman(1))
            {
                p1Controller.rectTransform.anchoredPosition += new Vector2(-400 ,0) ;
            }
            
        }
        
        if (Input.GetKeyDown(KeyCode.D) && p1Controller.rectTransform.anchoredPosition.x <= 0)
        {
            if (GameManager.Instance().SetPlayerJoker(1))
            {
                p1Controller.rectTransform.anchoredPosition += new Vector2(400, 0);
            }
        }
        
        if (Input.GetKeyDown(KeyCode.LeftArrow) && p2Controller.rectTransform.anchoredPosition.x >= 0)
        {
            if (GameManager.Instance().SetPlayerHitman(2))
            {
                p2Controller.rectTransform.anchoredPosition += new Vector2(-400, 0);
            }
        }
        
        if (Input.GetKeyDown(KeyCode.RightArrow) && p2Controller.rectTransform.anchoredPosition.x <= 0)
        {
            if (GameManager.Instance().SetPlayerJoker(2))
            {
                p2Controller.rectTransform.anchoredPosition += new Vector2(400, 0);
            }
        }*/
        
        
    }
}
